apiVersion: v1
kind: Service
metadata:
  name: my-service
spec:
  selector:
    app: myApp
  ports:
    - port: 80
      protocol: TCP
      targetPort: 9374
  # ports:
  #   - protocol: TCP
  #     port: 80
  #     targetPort: 9374